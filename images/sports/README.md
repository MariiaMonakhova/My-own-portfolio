# Sports photos

These feed the "Beyond Code" carousel (order & captions live in `src/data.ts`):

- `tennis-1.jpg`, `tennis-2.jpg`, `tennis-3.jpg`
- `yoga-1.jpg` … `yoga-6.jpg`

To add or swap a photo, drop the file here and update the matching entry in
`src/data.ts`. If a referenced file is missing, that slide gracefully shows a
gradient + icon placeholder instead of a broken image.
